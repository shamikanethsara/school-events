services:
  - type: web
    name: school-events-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: school-events-db
          property: connectionString

databases:
  - name: school-events-db
    plan: free
